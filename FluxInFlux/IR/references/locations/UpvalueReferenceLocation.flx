
import ReferenceLocation;

namespace Flux::IR;

class UpvalueReferenceLocation extends Flux::IR::ReferenceLocation {
	int layer;

	UpvalueReferenceLocation(int layer) {
		self.layer = layer;
	}
}

string UpvalueReferenceLocation:serialize()
	= "upvalue," `concat` self.layer;
