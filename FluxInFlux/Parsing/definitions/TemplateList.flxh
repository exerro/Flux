
import TemplateList;

namespace Flux::Parsing;

class TemplateList extends ASTNode {
	ASTNodeType type = Flux::Parsing::ASTNodeType.TemplateList;
	string[] names;
	Type[][] classes;
	
	TemplateList(string[] names, Type[][] classes) {
		self.names = names;
		self.classes = classes;
		self.position = null;
	}
}
