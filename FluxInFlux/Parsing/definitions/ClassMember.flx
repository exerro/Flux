
namespace Flux::Parsing;

string ClassMember:serialize()
	= (self.isPublic && "public " || "private ")
		`concat` (self.isStatic && "static " || "")
		`concat` (self.isConst && "const " || "")
		`concat` self.type:serialize()
		`concat` " " `concat` self.name `concat` ";";
