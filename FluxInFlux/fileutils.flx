
namespace Flux;

bool is_file(string file) {
	let h = io.open(file);
	if h {
		h:close();
		return true;
	}
	return false;
}

string[] list_files(string path) {
	return [];
}

string read_file(string file) {
	let h = io.open(file);
	let content = h:read "*a";

	h:close();

	return content;
}

void write_file(string file, content) {
	return null;
}
