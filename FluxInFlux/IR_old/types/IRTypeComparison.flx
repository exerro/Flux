
int{IRTypeComparison} IR_compare_weights = {
	[IRTypeComparison.ExactMatch] = 3;
	[IRTypeComparison.ExtendsFrom] = 2;
	[IRTypeComparison.CastsTo] = 1;
	[IRTypeComparison.NoMatch] = 0;
};

IRTypeComparison{int} IR_compare_lookup = {
	[3] = IRTypeComparison.ExactMatch;
	[2] = IRTypeComparison.ExtendsFrom;
	[1] = IRTypeComparison.CastsTo;
	[0] = IRTypeComparison.NoMatch;
};
