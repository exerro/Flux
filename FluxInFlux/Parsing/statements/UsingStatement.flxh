
namespace Flux::Parsing;

class UsingStatement extends Flux::Parsing::Statement {
	string name;

	UsingStatement(string name, Position position) {
		self.name = name;
		self.position = position;
	}
}
