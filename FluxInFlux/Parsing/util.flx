
namespace Flux::Parsing;

string getName(Source source) {
	let name = source.lexer:skipValue(Identifier);

	while name && source.lexer:skip(Symbol, "::")
		name = name `concat` "::" `concat` (source.lexer:skipValue(Identifier) || throw new ParserException("expected name after ':'"));
	
	return name;
}
