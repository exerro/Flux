
int x = 1;
const int y = 2;

float x = 1, y = 2;

void f()
	do int x = 1;
	then return x - 1;

UI::Element Button:addChild(UI::Element child) {
	self.children += child;
	child.parent = self;
	return child;
}

let x = function ->int (int x, int y)
	return x + y;
