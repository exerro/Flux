
namespace Flux::Parsing;

class OperatorExpression extends Flux::Parsing::ASTNode {
	string op;
	ASTNode[] operands;

	OperatorExpression(string op, ASTNode[] operands, Position position) {
		self.op = op;
		self.operands = operands;
		self.position = position;
	}
}
