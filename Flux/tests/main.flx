
import "parsing definitions";
import "parsing statements";
import "parsing expressions";
import "parsing classes";

// changed:

void x:operator+() {
	
}

class X::A (int, float) extends Y  implements abc, def, ghi {
	int x;

	void f() {
		return null;
	}

	auto operator+(A other) {
		return A(x + other.x);
	}

	A(int x = 0, null int y = 1) {
		return 2;
	}

	template<T>
	setter x(T _x)
		x = _x;

	getter x() = x;

	template<T>
	setter x(T _abc);

	getter y();
}
