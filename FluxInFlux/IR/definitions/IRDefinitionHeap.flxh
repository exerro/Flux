
// namespace Flux::Compiling;

import IRDefinition;

class IRDefinitionHeap {
	IRDefinition[] members;
	IRDefinitionPointer pointer = 0;

	IRDefinitionPointer define(IRDefinition d);
	IRDefinitionPointer[] lookup(string name, bool hasPrivate);
	IRDefinition get(int p);

	IRDefinitionHeap() {
		self.members = [];
	}
}
