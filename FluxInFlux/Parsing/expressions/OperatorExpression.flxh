
namespace Flux::Parsing;

class OperatorExpression extends Expression {
	string op;
	Expression[] operands;

	OperatorExpression(string op, Expression[] operands, Position position) {
		self.op = op;
		self.operands = operands;
		self.position = position;
	}
}
