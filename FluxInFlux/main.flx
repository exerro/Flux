
import flux;

let source = "
while true {
     print( 5 + 0xff );
	\"Hi\'!\";
	'\\n''a';
}
";

void main() {
	new Flux::Parsing::Lexer lexer( source );

	while !lexer:isEOF()
		print( lexer:next():tostring() );
}
