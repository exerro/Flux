
import Node;
import ReferenceLocation;

namespace Flux::IR;

/*abstract*/ class Reference extends Flux::IR::Node {
	ReferenceLocation location;

	bool exact_type(Type t);
	bool casts_type(Type t);

	void set_location(ReferenceLocation location);

	Node get_node();
}
